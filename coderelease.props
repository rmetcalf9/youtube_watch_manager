source ./_repo_vars.sh

export svnMode=RJMDOCKERSIMPLE

# must start with space
export RJMDOCKERSIMPLE_EXTRA_BUILD_ARGS=" --build-arg PYTHON_IMAGE=${PYTHON_IMAGE}"

export DOCKER_USERNAME=metcarob
export DOCKER_IMAGENAME=${PROJECT_NAME}-app
